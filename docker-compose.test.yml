services:
  php7.test:
    extends:
      file: docker-compose.release.yml
      service: php7
    build:
      target: test

  php8.test:
    extends:
      file: docker-compose.release.yml
      service: php8
    build:
      target: test

  node20.test:
    extends:
      file: docker-compose.release.yml
      service: node20
    build:
      target: test
